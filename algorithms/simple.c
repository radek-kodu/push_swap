#include "../push_swap.h"

void	sort_simple(t_node **a, t_node **b)
{
	// do this while get_stack_size(*a) > 3
		// find_min
		// get_node_position
		// if node_pos != 0
			// bring_node_top
		// pb
	// sort_three
	// do this while get_stack_size(*b) > 0
		// pa
}

